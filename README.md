# vllm_logit_processors



## Getting started

First start an interactive session using `run.sh`

Next, install the vllm branch/commit which supports budget control `install.sh`

Next, run `test_v0_lp.py`
